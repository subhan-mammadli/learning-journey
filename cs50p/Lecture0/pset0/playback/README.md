# 🎵 Playback Problem – Replace Spaces with Ellipsis

This is a solution to the **Playback problem** from CS50p.
The goal of the problem is to **replace spaces in user input with `...`** and display the result.

---

## 📝 Problem Description

Write a program that:

* Takes input from the user.
* Replaces all spaces with `...`.
* Prints the modified string.

---

## 💻 Solution

The solution can be found in [playback.py](./playback.py).

---

## 📌 Key Concepts

* **input()**: Reads a string from the user.
* **str.replace(" ", "...")**: Replaces all spaces with ellipses.
* **print()**: Displays the result.
